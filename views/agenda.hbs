{{> head title="Agenda" }}

	<div class="max-w-full min-h-screen bg-gray-800 lg:py-5 relative">

		<div class="flex justify-between h-10">
			<a href="/" class="w-16 ml-16 text-xs block bg-transparent hover:bg-green-600 text-green-600 font-semibold hover:text-white py-2 px-4 border border-green-700 hover:border-transparent rounded">logout</a>
			<h1 class="sm:text-2xl text-gray-500">Agenda de {{user.name}} </h1>
			<a href="/agenda/crear" class="w-32 text-xs mr-16 block bg-transparent hover:bg-blue-600 text-blue-600 font-semibold hover:text-white py-2 px-4 border border-blue-700 hover:border-transparent rounded">Añadir Contacto</a>
		</div>
				
		<div class=" max-w-screen-lg sm:flex mx-auto flex-wrap max-w-screen-sm justify-center lg:justify-between pt-6 px-6">
			{{#each listAgenda}}
				<div class="bg-white rounded-lg p-6 w-64 mx-auto sm:mx-auto my-5 relative">
				
					<div class="text-lg">{{name}}</div>
					<div class="text-purple-500">{{work}} </div>
					<div class="text-gray-600">{{email}}</div>
					<div class="text-gray-600">{{phone}}</div>
					<div class="absolute bottom-0 right-0 p-1">
						<a href="agenda/{{id}}" class="text-teal-400">editar</a> 
					</div>

				</div >
			{{/each}}
		</div>
	</div>

</body>
</html>